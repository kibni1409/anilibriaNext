import { API_CONFIG } from '@/config/api.config';

export const API_ENDPOINTS = {
  releases: API_CONFIG.endpoints.releases,
  search: API_CONFIG.endpoints.search,
  catalog: API_CONFIG.endpoints.catalog
};