'use client'

import styles from './Footer.module.scss';

const Footer = () => {
    return (
        <div className={styles.footerWrap}>
            Footer &copy; 2025
        </div>
    );
};

export default Footer;